# Shared UI Foundation

Phase 1 introduces the reusable UI component boundary for website standardization.

These components are intentionally presentation-neutral. Existing pages are not
rewritten in this phase.

## Standardization rule

Refactor implementation without changing existing UX/UI, business logic,
payment/inventory behavior, authentication, or customer flows.

Page migration happens in later phases, one page at a time.
